#include "main.h"
/**
 * mul - multiplies two integers
 * @a: the first input of the user
 * @b: the second input of the user
 * Return: shows 0 for success
 */
int mul(int a, int b)
{

	int result = a * b;

	return (result);
}
