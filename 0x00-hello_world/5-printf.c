#include <stdio.h>

/**
 * main - prints the given text followed by a new line
 *
 * Return: is always 0 showing success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
